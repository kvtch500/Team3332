---
author: claude
type: handoff
id: handoff618g
date: 2026-06-18
session: 11 (continues handoff618f)
---

# HANDOFF 618g — Thursday, June 18 2026 (session 11)

## Where Things Stand
618f (surface GPS errors during recording) is **committed and pushed** — confirmed at the top of
`origin/main` (commit `fcd33d8`, working tree was clean at session start). The stale 618d note
edit also got committed (`1fc791c`). Nothing was left hanging in the repo.

This session took the top backlog feature: **iOS Live Activity** — a Strava/Nike-style live card
on the lock screen + Dynamic Island showing live **distance / time / pace** during a run.

The work splits in two: a **JS half** (written + validated here, builds into the web app) and a
**native Swift half** (all source written, but the Widget Extension **target must be created in
Xcode on the Mac** — that's a GUI operation I can't do from here).

## What This Session Did

**JS bridge (`app/src/app.jsx`, frontend — needs rebuild):**
1. New pure helper `liveActivityPayload(actType, meters, elapsed)` → `{activityType, distanceMiles,
   elapsedSeconds, metricValue, metricLabel}` (pace for runs, mph for walks). Node-checkable.
2. New `LiveActivity` helper (sits next to `GeoTracker`) — feature-detected with the **same
   registerPlugin guard** (617d lesson: registerPlugin can throw and blank the app). Every call
   is wrapped so a sync throw or async rejection is swallowed; **no-op on web / Android / pre-16.1
   / before the Xcode target exists.**
3. Wired into `RecordRun`: `start()` → `LiveActivity.start`; the 1-second timer → `LiveActivity.update`
   with live distance+time+pace; `stop()` → `LiveActivity.end` (final stats); unmount → `end()` safety.

**Native Swift (NEW files, committed to `mobile/ios-native-src/` — see note below):**
- `Shared/RunActivityAttributes.swift` — shared ActivityKit model (attributes + ContentState).
  **Must be a member of BOTH the App and the widget targets.**
- `Team3332Widget/RunLiveActivity.swift` — SwiftUI lock-screen banner + Dynamic Island
  (compact/expanded/minimal), gold `#D4AF37` on dark `#0B0F14`.
- `Team3332Widget/Team3332WidgetBundle.swift` — widget `@main`.
- `App/LiveActivityPlugin.swift` + `App/LiveActivityPlugin.m` — Capacitor plugin
  (`registerPlugin('LiveActivity')`), `start`/`update`/`end`/`isSupported`, gated `#available(iOS 16.1)`.

> ⚠️ **`mobile/ios/` is gitignored** (regenerated by `cap add ios`), so the Swift source lives in
> the committed **`mobile/ios-native-src/`** instead. Xcode setup copies it into the project with
> "Copy items if needed" — details in `mobile/LIVE-ACTIVITY-SETUP.md`.

**Docs:** `mobile/LIVE-ACTIVITY-SETUP.md` (the one-time Xcode wiring), ROADMAP Live Activity
section updated.

## Validation
- `node --check` on the extracted `liveActivityPayload` + `LiveActivity` IIFE → **PASS**. ✓
- Behavior smoke test: Run 1mi/8min → `8:00` `/MI`; Walk 0.5mi/10min → `3.0` `MPH`; start → `0` /
  `—:—`. ✓
- Bracket-balance of `app/src/app.jsx` vs HEAD → **delta {par:0,brk:0,brc:0}** (edits net balanced). ✓
- **Could NOT build `app/app.js` in-sandbox**: npm registry is 403-blocked, so I can't install the
  Linux esbuild binary (the repo's is darwin-arm64). Confirms the standing "sandbox can't
  transpile" assumption. **Real proof = Mac `npm run build:app` + on-device check.** ✗ until done.
- Swift is **not** compiled here (no Xcode/sandbox) → proven only by the device build.

## ⚠️ What Ernest needs to do (on the Mac)

**This session changed `app/src/app.jsx` → must rebuild `app/app.js` before committing.** It also
adds new Swift files that need a one-time Xcode setup.

```bash
cd "/Users/ernestsmith/Desktop/claude ai/3332"

# 1. Rebuild the frontend bundle (app.jsx changed)
npm run build:app

# 2. Commit + push from the repo ROOT (path has a space)
git add app/src/app.jsx app/app.js ROADMAP.md \
        mobile/LIVE-ACTIVITY-SETUP.md mobile/ios-native-src \
        "daily update/handoff618g.md"
git commit -m "iOS Live Activity: lock-screen/Dynamic Island live run stats (plugin + widget + JS bridge)"
git push                    # deploys web app (JS bridge is a no-op on web, so site is unaffected)

# 3. One-time Xcode wiring — FOLLOW mobile/LIVE-ACTIVITY-SETUP.md
#    (create Widget Extension target, NSSupportsLiveActivities=YES, add plugin files to App,
#     tick RunActivityAttributes.swift into BOTH targets), then:
cd mobile && npm run sync && npm run open:ios   # ⌘R onto KATCH
```
**Test on device:** record a run, lock the phone → the TEAM 3332 card should show on the lock
screen / Dynamic Island with live distance·time·pace, updating ~1×/sec; hit END → it clears.
Reminder (FRONTEND-BUILD.md): never push `index.html`/`app.jsx` without a fresh `app/app.js`.

## Open Items (priority order)
- **Xcode wiring + device check for the Live Activity** (above) — the only thing between this code
  and a working card. The Swift won't compile until the widget target exists.
- **Still pending from 618e/f:** eyeball the Progress tab (real splits, no "est." on a fresh
  timestamped run) AND the 618f GPS-loss banner on device.
- **Phase 2 frontend pre-build** (optional) — bundle React/Leaflet/@capacitor/core locally, retire
  the head-loader.
- **Android live-stats analog** later: foreground-service ongoing notification (Android has no
  Live Activities).
- Carried: paid Apple Dev → TestFlight → wider device testing; Android; test-account deletion;
  lawyer ToS/Privacy; PostgreSQL migration ~2mo pre-launch.

## ⚠️ Working Agreements
- **Claude edits; Ernest commits/pushes from the Mac.** Repo path has a space; commit from the
  **root**, not `mobile/`.
- **Frontend is a build:** edit `app/src/app.jsx` (never `app/app.js`) → `npm run build:app` →
  commit both. Never push without a fresh `app/app.js`. (FRONTEND-BUILD.md)
- **Native rebuild = `cd mobile && npm run sync` THEN ⌘R** (sync builds app.js first).
- Sandbox can't transpile JS **and npm registry is blocked** — frontend validated by
  bracket-balance + `node --check` of extracted pure-JS; backend tests DO run in-sandbox.
- New API routes go **before `/:id`** in a router or they get captured as an id.
- **GPS errors are async → ErrorBoundary can't catch them.** Surface via state (618f).
- **New native plugins are feature-detected** with the registerPlugin guard and made no-op
  off-platform, so JS can ship before the native target is wired up (see `LiveActivity`).

## Start Here Next Time
1. Confirm 618g committed/pushed, the Xcode widget target is wired (LIVE-ACTIVITY-SETUP.md), and
   the Live Activity card shows/updates/clears on device.
2. Then pick: **Phase 2 frontend pre-build** · Android ongoing-notification analog.
3. Launch runway: paid Apple Dev → TestFlight → Android.

## Quick Reference
- Live: https://team3332.com (app at /app, admin at /admin) · Repo: github.com/kvtch500/Team3332
- Frontend: source `app/src/app.jsx` → built `app/app.js` (committed); shell `app/index.html`
  (CDN React/ReactDOM prod + Leaflet, then app.js). Build = `npm run build:app`. See FRONTEND-BUILD.md.
- **Live Activity (618g):** JS `LiveActivity` helper (registerPlugin-guarded, no-op off iOS) +
  `liveActivityPayload(actType,meters,elapsed)`; driven from `RecordRun` (start/1s-update/stop/unmount).
  Native: plugin `LiveActivityPlugin` (jsName `LiveActivity`) → `Activity<RunActivityAttributes>`;
  widget target `Team3332Widget`. One-time Xcode setup in `mobile/LIVE-ACTIVITY-SETUP.md`.
- GPS error UX (618f): `RecordRun` `gpsAlert` + `recGpsAlert(kind)` → red banner; auto-clears on
  next good fix. Wired through `GeoTracker.startRecording` `onError` (kinds 'denied'|'error').
- Progress API: `GET /api/activities/progress` (auth) → totals, streak, active_dates, best_efforts
  (real fastest-segment when track has `[lat,lon,t]`, else even-pace est.), predictions, monthly.
- Track shape: route_data `{source, points}`, points `[lat,lon]` (legacy) or `[lat,lon,t]` (618e+).
- Native: Capacitor 6, `com.team3332.app`, iOS at `mobile/ios`. Build = `cd mobile && npm run sync`
  then ⌘R. Device = KATCH, Personal Team signing. Background GPS verified 618b.
- Tests: `cd backend && node test/<file>` — full suite **136 green** (unchanged; 618g is frontend +
  new native, no backend touched).
- Prior sessions: handoff618f.md · handoff618e.md · handoff618d.md · handoff618c.md · handoff618b.md
